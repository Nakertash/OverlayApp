<Window x:Class="OverlayApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OverlayApp"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#00FFFFFF"
        Title="MainWindow" Height="120" Width="120">
    <Border CornerRadius="10" Background="#80000000" Padding="10" Margin="0,0,0,-20">
        <StackPanel>
            <!-- Верхняя панель для перетаскивания -->
            <StackPanel Orientation="Horizontal">
                <Border Background="#404040" Width="55" Height="30" MouseLeftButtonDown="DragArea_MouseLeftButtonDown"  MouseRightButtonUp="TitleBar_MouseRightButtonUp" Cursor="SizeAll">
                    <TextBlock Text="Overlay" Foreground="White" 
                    VerticalAlignment="Center" Margin="0,0,0,0" HorizontalAlignment="Center"/>
                </Border>
                <Button  Click="RefreshFile_Click" Background="Transparent">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Иконка -->
                        <Image Source="Assets/refresh.png" Width="20" Height="20"/>
                        <!-- Иконка -->
                    </StackPanel>
                </Button>
                <Button  Click="SettingsButton_Click" Background="Transparent">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Иконка -->
                        <Image Source="Assets/settingsIcon.png" Width="20" Height="20"/>
                        <!-- Иконка -->
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Контент -->
            <StackPanel >
                <Label x:Name="dpsLabel" Content="0" Foreground="White" HorizontalAlignment="Center" />
                <Label x:Name="timeLabel" Content="0" Foreground="White" HorizontalAlignment="Center" />
                <Button x:Name="StopStartButton" Content="Stop"  Click="Button_Click" Width="48"/>
            </StackPanel>
        </StackPanel>

    </Border>
</Window>
